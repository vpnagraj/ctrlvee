% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crawl_chunks.R
\name{crawl_chunks}
\alias{crawl_chunks}
\title{Fetch R code chunks from any URL}
\usage{
crawl_chunks(url, strategy = c("auto", "raw", "html"), verbose = TRUE)
}
\arguments{
\item{url}{Character vector of length 1 with the URL to fetch}

\item{strategy}{One of \code{"auto"} (default), \code{"raw"},  or
\code{"html"}. When \code{"auto"}, the strategy is detected from the URL
pattern using \code{\link[=detect_strategy]{detect_strategy()}}.}

\item{verbose}{Logical; default is \code{TRUE} and progress messages are printed}
}
\value{
A character vector of R code chunk bodies. Returns
\code{character(0)} if no R chunks are found.
}
\description{
The primary engine of the ctrlvee package. Give it a URL
to a raw source file, a GitHub Gist, or a rendered
HTML page and it returns the R code chunks found there.
}
\examples{
\dontrun{
## rendered Quarto book chapter (HTML strategy detected)
chunks <- crawl_chunks("https://r4ds.hadley.nz/data-visualize.html")


## GitHub Gist (plain .R file) (raw strategy detected)
chunks <- crawl_chunks(
  "https://gist.github.com/vpnagraj/59fa609c5adf47c8c7a5b156eb261be7"
)

## you can also dictate a specific strategy
chunks <- crawl_chunks("https://example.com/tutorial", strategy = "html")
}

}
