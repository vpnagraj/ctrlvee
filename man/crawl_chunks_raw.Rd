% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy_raw.R
\name{crawl_chunks_raw}
\alias{crawl_chunks_raw}
\title{Fetch and extract R code from a raw source URL}
\usage{
crawl_chunks_raw(url, verbose = TRUE)
}
\arguments{
\item{url}{Character vector of length 1 with the URL to fetch}

\item{verbose}{Logical; default is \code{TRUE} and progress messages are printed}
}
\value{
A character vector where each element is the body of one R
chunk. For plain \code{.R} files, a length-1 vector containing the
full file. Returns \code{character(0)} if the file is empty.
}
\description{
Downloads the raw text content of a URL (typically a \code{.qmd}, \code{.Rmd},
\code{.md}, or \code{.R} file served as plain text) and extracts R code.
}
\details{
For Markdown-family files, fenced R code blocks are extracted
individually. For plain \code{.R} files (or any file where no fenced
chunks are found), the entire file content is returned as a single
chunk.
}
\examples{
## fenced chunks from a Quarto source file
chunks <- crawl_chunks_raw(
  "https://raw.githubusercontent.com/hadley/r4ds/main/data-visualize.qmd"
)

## plain .R file from a GitHub Gist
chunks <- crawl_chunks_raw(
  "https://gist.githubusercontent.com/vpnagraj/59fa609c5adf47c8c7a5b156eb261be7/raw"
)

}
