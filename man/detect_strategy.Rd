% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_strategy.R
\name{detect_strategy}
\alias{detect_strategy}
\title{Detect the best extraction strategy for a URL}
\usage{
detect_strategy(url)
}
\arguments{
\item{url}{Character vector of length 1 with the URL to fetch}
}
\value{
One of \code{"raw"}, \code{"github"}, or \code{"html"}.
}
\description{
Examines the URL pattern and returns a strategy string that tells the
caller which extraction function to use. This will be one of either:
}
\details{
\describe{
\item{\code{"raw"}}{URL points directly to a raw source file or a GitHub
Gist. Parse fenced code blocks from the plain text, falling back to
treating the entire file as a single chunk for plain \code{.R} files.}
\item{\code{"html"}}{URL is a rendered web page (e.g. a Quarto book chapter
or pkgdown site). Parse \verb{<pre>/<code>} elements from the HTML.}
}

Detection is entirely URL-based (no network requests) and intentionally
conservative; when in doubt it falls back to \code{"html"}.

\strong{Rules (evaluated in order):}
\enumerate{
\item Contains \code{gist.githubusercontent.com} or matches
\verb{gist.github.com/<user>/<hash>}: \code{"raw"}
\item Contains \code{raw.githubusercontent.com} or ends in \code{.Rmd}, \code{.qmd},
\code{.md}, \code{.R}, or \code{.Rmarkdown}: \code{"raw"}
\item Everything else: \code{"html"}
}
}
\examples{
detect_strategy("https://gist.github.com/vpnagraj/59fa609c5adf47c8c7a5b156eb261be7")

detect_strategy("https://raw.githubusercontent.com/hadley/r4ds/main/data-visualize.qmd")

detect_strategy("https://r4ds.hadley.nz/data-visualize.html")

}
